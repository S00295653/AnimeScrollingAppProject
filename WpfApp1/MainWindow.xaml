<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AnimeScrollApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Anime Scroll" 
        Height="800" Width="450"
        WindowStyle="None" ResizeMode="NoResize"
        Background="Black">

    <Window.Resources>
        <Color x:Key="MainAccentColor">#E94560</Color>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource MainAccentColor}"/>

        <Style TargetType="Window">
            <Setter Property="MinHeight" Value="800"/>
            <Setter Property="MinWidth" Value="450"/>
        </Style>
    </Window.Resources>

    <Grid>
        <ScrollViewer x:Name="MainScrollViewer"
                      VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled"
                      PanningMode="None"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                      Focusable="False">
            <StackPanel>
                <StackPanel x:Name="AnimeContainer" 
                           MouseMove="AnimeContainer_MouseMove" 
                           MouseLeftButtonDown="AnimeContainer_MouseLeftButtonDown" 
                           MouseLeftButtonUp="AnimeContainer_MouseLeftButtonUp">
                </StackPanel>

                <Grid x:Name="LoadingArea" Height="300" Background="#F0000000" Visibility="Collapsed">
                    <Border Background="#1A1A1A" CornerRadius="20" Margin="30,50" 
                            BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Grid Width="50" Height="50" Margin="0,0,0,15">
                                <Ellipse Stroke="{StaticResource AccentBrush}" StrokeThickness="3" Opacity="0.1"/>
                                <Ellipse x:Name="LoadingCircle" Stroke="{StaticResource AccentBrush}" 
                                         StrokeThickness="3" StrokeDashArray="2,1" 
                                         RenderTransformOrigin="0.5,0.5">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform x:Name="SpinTransform" Angle="0"/>
                                    </Ellipse.RenderTransform>
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="SpinTransform" Storyboard.TargetProperty="Angle"
                                                                     From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>
                            </Grid>
                            <TextBlock Text="Chargement..." FontSize="16" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Exploration d'AniList" FontSize="12" Foreground="#808080" Margin="0,2,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <Grid VerticalAlignment="Top" HorizontalAlignment="Right" Margin="15">
            <StackPanel>
                <Button Content="✕" Click="CloseButton_Click" Width="35" Height="35" Foreground="White" Background="#80000000" BorderThickness="0" Cursor="Hand" Margin="0,0,0,10">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="18">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Border Background="#A0000000" CornerRadius="15" Padding="10,5">
                    <TextBlock x:Name="AnimeCounter" Text="0 animes" Foreground="White" FontSize="11" FontWeight="Bold"/>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
